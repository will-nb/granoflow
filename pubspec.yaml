name: granoflow
description: Offline-first task and timer manager.
publish_to: 'none'
version: 0.1.1+2

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  collection: ^1.19.0
  path_provider: ^2.1.2
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  provider: ^6.1.1
  drift: ^2.29.0
  sqlite3_flutter_libs: ^0.5.40
  sqlite3: ^2.5.0
  go_router: ^16.3.0
  flutter_svg: ^2.0.10+1
  audioplayers: ^6.5.1
  sleek_circular_slider: ^2.0.1
  flutter_local_notifications: ^19.5.0
  flutter_local_notifications_linux: ^6.0.0
  flutter_local_notifications_windows: ^1.0.3
  flutter_foreground_task: ^8.17.0
  wakelock_plus: ^1.2.0
  timezone: ^0.10.1
  share_plus: ^10.1.2
  uuid: ^4.0.0
  archive: ^3.4.0
  file_picker: ^8.1.4
  shared_preferences: ^2.3.3
  mask_text_input_formatter: ^2.9.0
  encrypt: ^5.0.3
  crypto: ^3.0.5
  table_calendar: ^3.2.0
  flutter_week_view: ^1.4.3
  flutter_quill: ^11.0.0
  tray_manager: ^0.2.0
  window_manager: ^0.5.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  build_runner: ^2.4.11
  drift_dev: ^2.29.0
  msix: ^3.16.12

msix_config:
  display_name: "GranoFlow"
  publisher_display_name: "GranoFlow Team"
  identity_name: "com.granoflow.lite"
  publisher: "CN=GranoFlow Test Publisher"
  msix_version: 1.0.0.0
  certificate_path: "windows/granoflow-test-cert.pfx"
  certificate_password: "uKcaZRFLezfR68aW"
  start_menu: true
  desktop_shortcut: true
  architecture: x64
  install_certificate: false

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/seeds/version.json
    - assets/seeds/en/
    - assets/seeds/zh_CN/
    - assets/seeds/zh_HK/
    - assets/images/background.light.webp
    - assets/images/background.dark.webp
    - assets/images/homepage_empty_data.webp
    - assets/logo/
    - assets/sounds/
    - assets/config/desktop_window.json
    - assets/config/calendar_review_heatmap_thresholds.json
    - assets/config/rich_text_editor.json
